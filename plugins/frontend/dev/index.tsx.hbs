import { createDevApp } from '@backstage/dev-utils';
import { datacontract, {{ extensionName }} } from '../src/plugin';

createDevApp()
  .registerPlugin(datacontract)
  .addPage({
    element: <{{ extensionName }} />,
    title: 'Root Page',
    path: '/backstage-plugin-datacontract',
  })
  .render();
